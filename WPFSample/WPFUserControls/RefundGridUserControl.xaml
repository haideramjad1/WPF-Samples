<UserControl x:Class="WPFSample.WPFUserControls.RefundGridUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFSample.WPFUserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!--<Border BorderThickness="1" Padding="5" Background="Transparent">
        <TextBlock Text="{Binding Text}" 
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"/>
    </Border>-->

    <Border BorderThickness="1" Padding="5" Background="Transparent">
        <TextBlock Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"/>
    </Border>

</UserControl>
