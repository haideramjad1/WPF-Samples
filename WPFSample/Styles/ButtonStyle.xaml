<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ImageBrush x:Key="PrevAct" ImageSource="/Resources/2222crop 1.png"/>
    <ImageBrush x:Key="PrevPas" ImageSource="/Resources/customer_icon.png"/>
    <ImageBrush x:Key="CloseButtonImageBrush" ImageSource="/Resources/Close_32x32.png"/>
    <ImageBrush x:Key="MagnifyButtonImageBrush" ImageSource="/Resources/Magnifier.png"/>
    <ImageBrush x:Key="NextButtonImageBrush" ImageSource="/Resources/next.png" />
    <ImageBrush x:Key="PrevButtonImageBrush" ImageSource="/Resources/previous.png" />

    <ImageBrush x:Key="OpenEyeImageBrush" ImageSource="/Resources/OpenEye_Image.png" />
    <ImageBrush x:Key="ClosedEyeImageBrush" ImageSource="/Resources/ClosedEye_Image.png" />
    


    <Style x:Key="AllButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Height" Value="50"/>
        <Setter Property="Width" Value="50"/>
        <Setter Property="Margin" Value="50"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Border"
                            BorderThickness="0"
                            Background="{StaticResource PrevPas}"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="Border" Value="{StaticResource PrevAct}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" TargetName="Border" Value="{StaticResource PrevAct}"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="KeyboardButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="FontSize" Value="25" />
        <Setter Property="FontFamily" Value="Tahoma" />
        <Setter Property="Margin" Value="5,5,5,5" />
        <Setter Property="Background" Value="#eee"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                CornerRadius="5"
                Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="LightGray"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="5" ShadowDepth="5" Direction="270" Opacity="0.5"/>
            </Setter.Value>
        </Setter>

        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="Button.Click">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                             To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                             To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>

    </Style>

    <Style x:Key="NumericButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="50" />
        <Setter Property="FontFamily" Value="Tahoma" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Margin" Value="5" />
        <Setter Property="Background" Value="MidnightBlue"/>

        <Setter Property ="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                        CornerRadius="15"
                        Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder"
                                Property="Background"
                                Value="#111362"/>
                        </Trigger>
                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="Button.Click">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                     To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                     To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
        
    </Style>
    
    <Style x:Key="LeftTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="-5,0,0,0"/>
    </Style>

    <Style x:Key="RightTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Foreground" Value="#0A56C4"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="20,5,0,0"/>
    </Style>


    

    <SolidColorBrush x:Key="DefaultBackground" Color="#eee"/>
    <SolidColorBrush x:Key="DefaultHoverBackground" Color="LightGray" />
    <SolidColorBrush x:Key="WhiteBackground" Color="White"/>

    <SolidColorBrush x:Key="LightBlueBackground" Color="#EFF2F8"/>

    <LinearGradientBrush x:Key="SkyBlueGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#3098B0" Offset="0"/>
        <GradientStop Color="#46CECA" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="SkyBlueGreenHoverBackground" Color="MediumTurquoise" />

    <LinearGradientBrush x:Key="SkyBlueLightGreenGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#3F8C8E" Offset="0"/>
        <GradientStop Color="#64CAA8" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="SkyBlueLightGreenHoverBackground" Color="MediumAquamarine" />

    <LinearGradientBrush x:Key="LightSkyBlueGreenGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#257A5E" Offset="0"/>
        <GradientStop Color="#3BB070" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="LightSkyBlueGreenHoverBackground" Color="MediumSeaGreen" />
    
    <LinearGradientBrush x:Key="GreenGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#275D28" Offset="0"/>
        <GradientStop Color="#3E862F" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="GreenHoverBackground" Color="Green" />

    <LinearGradientBrush x:Key="RedGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#810304" Offset="0"/>
        <GradientStop Color="#D20404" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="RedHoverBackground" Color="Red" />
    
    <LinearGradientBrush x:Key="MusteredGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <!--<GradientStop Color="#810304" Offset="0"/>-->
        <GradientStop Color="#f4b41a" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="MusteredHoverBackground" Color="#f4b41a" />
    
    <LinearGradientBrush x:Key="BlueGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#1476FB" Offset="0"/>
        <GradientStop Color="#1D8EFF" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="BlueHoverBackground" Color="Blue" />
    
    <LinearGradientBrush x:Key="DarkBlueGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#265187" Offset="0"/>
        <GradientStop Color="#386B9A" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="DarkBlueHoverBackground" Color="#265187" />
    
    <LinearGradientBrush x:Key="OrangeGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#875000" Offset="0"/>
        <GradientStop Color="#E58000" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="Orange1GradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#E58000" Offset="0"/>
        <GradientStop Color="#E58000" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="OrangeHoverBackground" Color="Orange" />

    <LinearGradientBrush x:Key="YellowGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FFFF02" Offset="0"/>
        <GradientStop Color="#FFFFD8" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="YellowHoverBackground" Color="Yellow" />
    
    <LinearGradientBrush x:Key="ParrotGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <!--<GradientStop Color="#56BA6E" Offset="0"/>-->
        <GradientStop Color="#8BF75A" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="ParrotHoverBackground" Color="LawnGreen" />

    <LinearGradientBrush x:Key="ManagerBlueGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#0052B4" Offset="0"/>
        <GradientStop Color="#0071D1" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="ManagerBlueHoverBackground" Color="Blue" />
    
    <LinearGradientBrush x:Key="ManagerTeelGradientBackground" StartPoint="0,0" EndPoint="0,1">
        <!--<GradientStop Color="#0052B4" Offset="0"/>-->
        <GradientStop Color="#143d59" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="ManagerTeelHoverBackground" Color="#143d59" />

    <Style x:Key="BaseKeyboardButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="FontSize" Value="25" />
        <Setter Property="FontFamily" Value="Tahoma" />
        <Setter Property="Margin" Value="5,5,5,5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="{StaticResource DefaultBackground}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                        CornerRadius="5"
                        Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultHoverBackground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="5" ShadowDepth="5" Direction="270" Opacity="0.5"/>
            </Setter.Value>
        </Setter>

        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="Button.Click">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                     To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                     To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="GreyKeyboardButtonStyle" BasedOn="{StaticResource BaseKeyboardButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource DefaultBackground}" />
        <Setter Property="Tag" Value="{StaticResource DefaultHoverBackground}" />
    </Style>

    <Style x:Key="CustomerOrProductButtonStyle" BasedOn="{StaticResource BaseKeyboardButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource LightBlueBackground}" />
        <Setter Property="Tag" Value="{StaticResource DefaultHoverBackground}" />
    </Style>



    <Style x:Key="BaseAllButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontFamily" Value="Tahoma" />
        <Setter Property="Margin" Value="5,5,5,5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="{StaticResource DefaultBackground}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                    CornerRadius="15"
                    Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="5" ShadowDepth="5" Direction="270" Opacity="0.5"/>
            </Setter.Value>
        </Setter>

        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="Button.Click">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                 To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                 To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>
    
    <Style x:Key="UserMangerButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="25" />
        <Setter Property="FontFamily" Value="Tahoma" />
        <Setter Property="Margin" Value="5,5,5,5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="{StaticResource DefaultBackground}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                    CornerRadius="10"
                    Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="5" ShadowDepth="5" Direction="270" Opacity="0.5"/>
            </Setter.Value>
        </Setter>

        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="Button.Click">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                 To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                 To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SkyBlueButtonStyle" BasedOn="{StaticResource BaseAllButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource SkyBlueGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource GreenHoverBackground}" />
    </Style>

    <Style x:Key="SkyBlueLightGreenButtonStyle" BasedOn="{StaticResource BaseAllButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource SkyBlueLightGreenGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource GreenHoverBackground}" />
    </Style>

    <Style x:Key="LightSkyBlueGreenButtonStyle" BasedOn="{StaticResource BaseAllButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource LightSkyBlueGreenGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource GreenHoverBackground}" />
    </Style>
    
    <Style x:Key="GreenButtonStyle" BasedOn="{StaticResource BaseAllButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource GreenGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource GreenHoverBackground}" />
    </Style>

    <Style x:Key="RedButtonStyle" BasedOn="{StaticResource BaseAllButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource RedGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource RedHoverBackground}" />
    </Style>

    <Style x:Key="BlueButtonStyle" BasedOn="{StaticResource BaseAllButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BlueGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource BlueHoverBackground}" />
    </Style>

    <Style x:Key="YellowButtonStyle" BasedOn="{StaticResource BaseAllButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource YellowGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource YellowHoverBackground}" />
        <Setter Property="Foreground" Value="Black" />
    </Style>

    <Style x:Key="ParrotButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ParrotHoverBackground}" />
        <Setter Property="Tag" Value="{StaticResource ParrotGradientBackground}" />
        <Setter Property="Foreground" Value="Black" />
    </Style>

    <Style x:Key="DarkBlueButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource DarkBlueGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource DarkBlueHoverBackground}" />
        <Setter Property="Margin" Value="5,6,5,6"/>
    </Style>
    
    <Style x:Key="OrangeButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource OrangeGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource OrangeHoverBackground}" />
        <Setter Property="Margin" Value="5,6,5,6"/>
    </Style>

    <Style x:Key="ManagerBlueButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ManagerBlueGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource BlueHoverBackground}" />
    </Style>
    
    <Style x:Key="ManagerTeelButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ManagerTeelGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource ManagerTeelHoverBackground}" />
    </Style>
    
    <Style x:Key="ManagerLightBlueButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BlueGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource BlueHoverBackground}" />
    </Style>
    
    <Style x:Key="ManagerGrayButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="White" />
        <Setter Property="Tag" Value="LightGray" />
    </Style>
    
    <Style x:Key="ManagerDarkGrayButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="LightGray" />
        <Setter Property="Tag" Value="LightGray" />
    </Style>
    
    <Style x:Key="ManagerOrangeButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Orange1GradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource OrangeHoverBackground}" />
    </Style>

    <Style x:Key="ManagerGreenButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource GreenGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource GreenHoverBackground}" />
    </Style>

    <Style x:Key="ManagerRedButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource RedGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource RedHoverBackground}" />
    </Style>
    
    <Style x:Key="ManagerMusteredButtonStyle" BasedOn="{StaticResource UserMangerButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource MusteredGradientBackground}" />
        <Setter Property="Tag" Value="{StaticResource MusteredHoverBackground}" />
    </Style>

    <Style x:Key="LeftTextKeyBoardStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="-5,0,0,0"/>
    </Style>

    <Style x:Key="RightTextKeyBoardStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Foreground" Value="#0A56C4"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="20,5,0,0"/>
    </Style>

    <ControlTemplate x:Key="ImageButtonTemplate" TargetType="Button">
        <Border x:Name="BackgroundBorder" Background="{TemplateBinding Tag}" CornerRadius="5">

            <Border.Effect>
                <DropShadowEffect BlurRadius="5" ShadowDepth="5" Direction="270" Opacity="0.5" />
            </Border.Effect>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="BackgroundBorder" Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="10" Direction="270" Opacity="0.7" />
                    </Setter.Value>
                </Setter>
            </Trigger>

            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="BackgroundBorder" Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="7" ShadowDepth="7" Direction="270" Opacity="0.6" />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    
    <!--this is the style used in login form pasword hide or show-->
    <Style x:Key="LogInImageButtonStyle" TargetType="Button">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Tag}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="Button.Click">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                     To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                     To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <!--this is the style for text which will be used for the names-->
    <Style x:Key="LeftAlignedTextBlock" TargetType="TextBlock">
        <Setter Property="FontSize" Value="25"/>
        <Setter Property="FontFamily" Value="Tahoma"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="20,0,0,0"/>

    </Style>

    <!-- this is the style for text which will be used for the names to be left align with font 20 -->
    <Style x:Key="LeftAlignedTextBlock20" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontFamily" Value="Tahoma"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="20,0,0,0"/>
    </Style>

    <!-- this is the style for text which will be used for the right align with font 20 -->
    <Style x:Key="RightAlignedTextBlock20" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontFamily" Value="Tahoma"/>
        <Setter Property="TextAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="20,0,0,0"/>
    </Style>

    <!--this is the style for text box to have curve corners-->
    <Style x:Key="CommonTextBoxStyle" TargetType="TextBox">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="Tahoma" />
        <Setter Property="FontSize" Value="25" />
        <Setter Property="Margin" Value="10,0,10,0" />
        <Setter Property="Height" Value="40" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="10"
                    Padding="10,3,0,0">
                        <ScrollViewer Margin="0" 
                      x:Name="PART_ContentHost" 
                      VerticalScrollBarVisibility="Hidden" 
                      HorizontalScrollBarVisibility="Hidden" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--this is the code style used for PlaceHolder-->
    <Style x:Key="PlaceholderTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontFamily" Value="Tahoma" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="Gray" />
        <Setter Property="Margin" Value="15,0,10,0" />
        <Setter Property="IsHitTestVisible" Value="False" />
    </Style>

    <!--this is the style for Date Picker-->
    <Style x:Key="CustomDatePickerStyle" TargetType="DatePicker">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DatePicker">
                    <Border CornerRadius="10"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                            CornerRadius="10"
                            Background="White"
                            BorderThickness="1"
                            Margin="0,0,10,0">
                                <DatePickerTextBox x:Name="PART_TextBox"
                                           IsReadOnly="True"
                                           VerticalContentAlignment="Center"
                                           FontSize="20"
                                           Margin="5,0,0,0"
                                           FontFamily="{TemplateBinding FontFamily}"
                                           Background="Transparent"
                                           Foreground="Gray"/>
                            </Border>

                            <Button x:Name="PART_Button"
                            Grid.Column="1"
                            Margin="0,0,5,0"
                            Width="40"
                            HorizontalAlignment="Center"
                            BorderBrush="Transparent"
                            Background="Transparent"
                            FocusVisualStyle="{x:Null}">

                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="CalenderBorder" 
                                        CornerRadius="5"
                                        Background="White">
                                            <ContentControl Content="📅" 
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="CalenderBorder" Property="Background" Value="LightGray" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>

                            </Button>

                            <Popup x:Name="PART_Popup"
                                   Height="500"
                                   Width="450"
                           AllowsTransparency="True"
                           Placement="Bottom"
                           StaysOpen="False"
                           IsOpen="{Binding IsDropDownOpen, 
                           RelativeSource={RelativeSource TemplatedParent}}">
                                <Calendar x:Name="PART_Calendar" />
                            </Popup>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    
        <!-- Custom DatePicker Style -->
    <Style x:Key="CustomDatePickerStyle1" TargetType="DatePicker">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DatePicker">
                    <Border CornerRadius="10"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    CornerRadius="10"
                                    Background="White"
                                    BorderThickness="1"
                                    Margin="0,0,10,0">
                                <DatePickerTextBox x:Name="PART_TextBox"
                                                   IsReadOnly="True"
                                                   VerticalContentAlignment="Center"
                                                   FontSize="20"
                                                   Margin="5,0,0,0"
                                                   FontFamily="{TemplateBinding FontFamily}"
                                                   Background="Transparent"
                                                   Foreground="Gray"/>
                            </Border>

                            <Button x:Name="PART_Button"
                                    Grid.Column="1"
                                    Margin="0,0,5,0"
                                    Width="40"
                                    HorizontalAlignment="Center"
                                    BorderBrush="Transparent"
                                    Background="Transparent"
                                    FocusVisualStyle="{x:Null}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="CalenderBorder" 
                                                CornerRadius="5"
                                                Background="White">
                                            <ContentControl Content="📅" 
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="CalenderBorder" Property="Background" Value="LightGray" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="BasesKeyboardButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="FontSize" Value="25" />
        <Setter Property="FontFamily" Value="Tahoma" />
        <Setter Property="Margin" Value="5,5,5,2" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="{StaticResource DefaultBackground}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
            CornerRadius="80"
            Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultHoverBackground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="Button.Click">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
         To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
         To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="LightGreyKeyboardButtonStyle" BasedOn="{StaticResource BasesKeyboardButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource DefaultBackground}" />
        <Setter Property="Tag" Value="{StaticResource DefaultHoverBackground}" />
    </Style>

    <SolidColorBrush x:Key="greyBackground" Color="#2D2825"/>
    <SolidColorBrush x:Key="greyshadeBackground" Color="#868382"/>

    <Style x:Key="BaseShiftKeyboardButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="25" />
        <Setter Property="FontFamily" Value="Tahoma" />
        <Setter Property="Margin" Value="5,5,5,5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="{StaticResource DefaultBackground}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                CornerRadius="25"
                Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="Button.Click">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
             To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
             To="0.9" Duration="0:0:0.1" AutoReverse="True" FillBehavior="Stop"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="KeyboardBlackButtonStyle" BasedOn="{StaticResource BaseShiftKeyboardButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource greyBackground}" />
        <Setter Property="Tag" Value="{StaticResource greyshadeBackground}" />
    </Style>
    
    <Style x:Key="KeyboardwhiteButtonStyle" BasedOn="{StaticResource BaseShiftKeyboardButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource DefaultBackground}" />
        <Setter Property="Tag" Value="{StaticResource DefaultHoverBackground}" />
    </Style>

    <!--this is the style for Combo box-->
    <Style x:Key="CustomComboBoxStyle" TargetType="ComboBox">
        <Setter Property="FontSize" Value="25" />
        <Setter Property="FontFamily" Value="Tahoma" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border Background="White"
                        BorderBrush="LightGray"
                        BorderThickness="1"
                        CornerRadius="10">
                            <Grid>
                                <ContentPresenter x:Name="ContentPresenter"
                                          ContentTemplate="{TemplateBinding ItemTemplate}"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,0,30,0" />

                                <ToggleButton x:Name="ToggleButton"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                      Focusable="False"
                                      ClickMode="Press"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Center"
                                      Width="30"
                                      Height="30"
                                      Margin="0,0,5,0">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Grid>
                                                <Border Background="White"
                                                BorderBrush="Gray"
                                                BorderThickness="2"
                                                CornerRadius="5">
                                                    <Path x:Name="Arrow"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Data="M0,0 L5,10 L10,0 Z"
                                                  Fill="Black" />
                                                </Border>
                                            </Grid>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                            </Grid>
                        </Border>

                        <Popup x:Name="PART_Popup"
                       Placement="Bottom"
                       StaysOpen="False"
                       IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                       AllowsTransparency="True">
                            <Border Background="White"
                            BorderBrush="LightGray"
                            BorderThickness="1"
                            CornerRadius="5"
                            MinWidth="{TemplateBinding ActualWidth}">

                                <ScrollViewer>
                                    <ItemsPresenter x:Name="PART_ItemsPresenter" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- this is the style for information button having a transparent image -->
    <Style x:Key="RoundedTransparentButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Margin" Value="0,12,10,12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="20">

                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--<Style x:Key="RingButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        --><!-- Outer Ring (Static) --><!--
                        <Image Source="/Resources/OuterRing_Image_wider.png"/>

                        --><!-- Inner Image with proper binding --><!--
                        <Image Source="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Tag}" 
                           x:Name="InnerImage" 
                           RenderTransformOrigin="0.5,0.5">
                            <Image.RenderTransform>
                                <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1"/>
                            </Image.RenderTransform>
                        </Image>

                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform" 
                                                   Storyboard.TargetProperty="ScaleX" 
                                                   To="0.9" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform" 
                                                   Storyboard.TargetProperty="ScaleY" 
                                                   To="0.9" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform" 
                                                   Storyboard.TargetProperty="ScaleX" 
                                                   To="1" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform" 
                                                   Storyboard.TargetProperty="ScaleY" 
                                                   To="1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->

    <Style x:Key="RingButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- Static Outer Ring (No Transform) -->
                        <Image Source="/Resources/OuterRing_Image_wider.png"/>

                        <!-- Inner Content Group (Scales when pressed) -->
                        <Grid x:Name="InnerGroup" RenderTransformOrigin="0.5,0.5">
                            <Grid.RenderTransform>
                                <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1"/>
                            </Grid.RenderTransform>

                            <!-- Inner Ring from Tag -->
                            <Image Source="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Tag}" />

                            <!-- ContentPresenter shows <Image> in Button -->
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>

                    <!-- Only animate InnerGroup (not the outer ring) -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform" 
                                                     Storyboard.TargetProperty="ScaleX" 
                                                     To="0.9" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform" 
                                                     Storyboard.TargetProperty="ScaleY" 
                                                     To="0.9" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform" 
                                                     Storyboard.TargetProperty="ScaleX" 
                                                     To="1" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform" 
                                                     Storyboard.TargetProperty="ScaleY" 
                                                     To="1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- this is the style for the buttons in refund form that are used to be dynamicaly created -->
    <Style x:Key="RefundButtonStyle" BasedOn="{StaticResource BaseKeyboardButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource WhiteBackground}" />
        <Setter Property="Tag" Value="{StaticResource DefaultHoverBackground}" />
    </Style>
    
</ResourceDictionary>